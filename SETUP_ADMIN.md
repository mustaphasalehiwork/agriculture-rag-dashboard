# 🚀 راهنمای سریع ایجاد کاربر ادمین

## 📋 مراحل ساده برای راه‌اندازی

### ۱. راه‌اندازی پروژه
```bash
# نصب پکیج‌ها
npm install

# ایجاد فایل env
cp .env.local.example .env.local

# تنظیم متغیرهای Supabase در .env.local:
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# اجرای پروژه
npm run dev
```

### ۲. ایجاد اولین کاربر (ادمین)

1. **پروژه را اجرا کنید:** `npm run dev`
2. **به صفحه ورود بروید:** `http://localhost:3001/login`
3. **تب "ثبت‌نام" را انتخاب کنید**
4. **فرم را پر کنید:**
   - ایمیل: `your-email@example.com` (ایمیل دلخواه خود)
   - رمز عبور: `YourSecurePassword123` (رمز قوی)
5. **روی "ایجاد حساب کاربری" کلیک کنید**
6. **ایمیل خود را تایید کنید** (لینک تایید به ایمیل شما ارسال می‌شود)

### ۳. تبدیل کاربر به ادمین

در فایل `src/contexts/AuthContext.tsx`، ایمیل خود را به لیست ادمین‌ها اضافه کنید:

```typescript
const adminEmails = [
  'your-email@example.com',  // ← ایمیل خود را اینجا اضافه کنید
  'admin@example.com',
  'admin@agriculture-dashboard.com'
];
```

### ۴. لاگین به عنوان ادمین

1. **با ایمیل و رمز عبوری که ساختید وارد شوید**
2. **به داشبورد بروید**
3. **در سایدبار، گزینه "مدیریت کاربران" را خواهید دید** (فقط برای ادمین‌ها)

---

## ✅ تمام شد!

حالا شما:
- ✅ یک کاربر ادمین دارید
- ✅ می‌توانید کاربران جدید ایجاد کنید
- ✅ به صفحه مدیریت کاربران دسترسی دارید

---

## 🛠️ مدیریت کاربران (فقط ادمین)

از صفحه `/dashboard/users` می‌توانید:
- کاربران جدید ایجاد کنید
- لیست کاربران را مشاهده کنید
- کاربران را حذف کنید (به جز خودتان)

---

## 🔧 مشکلات احتمالی

### کاربر وارد نمی‌شود؟
- مطمئن شوید ایمیل را تایید کرده‌اید
- ایمیل خود را در لیست `adminEmails` چک کنید

### ایمیل تایید نیامد؟
- در Supabase Dashboard → Authentication → Settings، گزینه "Disable email confirmations" را فعال کنید
- یا از ایمیل‌های تستی مانندtemp-mail.org استفاده کنید

---

## 📝 اطلاعات کاربر ادمین نمونه

اگر می‌خواهید از مقادیر پیش‌فرض استفاده کنید:
- ایمیل: `admin@example.com`
- رمز عبور: `Admin123456`

این مقادیر را هم در `AuthContext.tsx` و هم در فرم ثبت‌نام استفاده کنید.